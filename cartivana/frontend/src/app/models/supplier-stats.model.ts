export interface ProductStats {
    total: number;
    approved: number;
    pending: number;
    rejected: number;
  }
  
  export interface OrderStats {
    totalOrders: number;
    monthlyOrders: number[];
  }
  